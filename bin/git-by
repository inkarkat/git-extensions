#!/bin/bash

typeset -ra scopeCommand=(byuserrange-command)
readonly scopeWhat='changes committed consecutively by the author of the passed -r|--revision REVISION / the most recent revision from the passed --author / --committer(s)'

typeset -ra scopeCommandLogArgs=(--with-log-args)
typeset -ra scopeCommandLogAndLastArgs=(--with-log-args-except-last)
readonly scopeArgs='[-r|--revision REVISION] [--author=<pattern>|--committer=<pattern> ...]'
readonly scopeInoutNote='does not make sense because the second revision always is an ancestor of the first.'

readonly scriptDir="$([ "${BASH_SOURCE[0]}" ] && dirname -- "${BASH_SOURCE[0]}" || exit 3)"
[ -d "$scriptDir" ] || { echo >&2 'ERROR: Cannot determine script directory!'; exit 3; }
readonly projectDir="${scriptDir}/.."
readonly libDir="${projectDir}/lib/br-variants"

source "${libDir}/custom-range.sh"
