#!/bin/sh

GIT_SHOWFILES_COMMAND=git-changesetfiles \
GIT_SHOWFILES_USAGE_DELEGATION_COMMAND='git-changesetfiles --understands filter' \
GIT_SHOWFILES_WHAT='files that often get changed in commits that modify files (inside <pathspec>)' \
exec git-showfiles-command "$@"
