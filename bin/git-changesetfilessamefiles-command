#!/bin/sh

GIT_CHANGESETFILESCOMMAND_SUFFIX_ARGS='--for-commits' \
GIT_SHOWFILES_WHAT="files (inside <pathspec>) that were modified in the last commit / N'th commit / passed -r|--revision[s] REVISION(s) / revision range" \
exec git-changesetfiles-command "$@"
