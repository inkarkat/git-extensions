#!/bin/bash

GIT_LOGRANGE_RANGE_WHAT='the actual diff; i.e. those filepairs whose preimage and postimage have different number of occurrences of the text or /regexp/' \
GIT_LOGRANGE_RANGE_COMMAND=logchanged \
    exec git-logrange-command "$@"
