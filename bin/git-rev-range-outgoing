#!/bin/bash

remote=$1; shift
if [ "$remote" ]; then
    remoteBranch=$(git-remotebr "$remote")
else
    remoteBranch=$(git-tracks)
fi || exit $?
exec git-branch-command --keep-position rev-range --revision "$remoteBranch" --end-revision HEAD "$@"
