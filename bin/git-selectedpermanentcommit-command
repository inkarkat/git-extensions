#!/bin/sh

GIT_SELECTEDCOMMIT_COMMAND="${GIT_SELECTEDCOMMIT_COMMAND:-log} --diff-merges=first-parent --no-patch --grep '^\\(fixup\\|amend\\|squash\\)! ' --invert-grep" \
    exec git-selectedcommit-command "$@"
