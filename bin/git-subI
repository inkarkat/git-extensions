#!/bin/bash
set -o pipefail

printUsage()
{
    cat <<HELPTEXT
Show a list of submodules that are currently modified but not staged in the
superproject.
HELPTEXT
    echo
    printf 'Usage: %q %s\n' "$(basename "$1")" '[--] [<path>...] [-?|-h|--help]'
}
case "$1" in
    --help|-h|-\?)	shift; printUsage "$0"; exit 0;;
esac

git-stIfiles --ignore-submodules=all "$@" | \
    git-subfilter
