#!/bin/bash

typeset -ra scopeCommand=(branch-command --mandatory-branch --keep-position rev-range --revision BRANCH)
readonly scopeWhat='the additions of the current branch versus the passed via -b|--branch branch'

readonly scriptDir="$([ "${BASH_SOURCE[0]}" ] && dirname -- "${BASH_SOURCE[0]}" || exit 3)"
[ -d "$scriptDir" ] || { echo >&2 'ERROR: Cannot determine script directory!'; exit 3; }
readonly projectDir="${scriptDir}/.."
readonly libDir="${projectDir}/lib/br-variants"

source "${libDir}/custom-range.sh"
