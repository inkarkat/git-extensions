#!/bin/bash
set -o pipefail

: ${GIT_TIMESPAN_MIDNIGHT:=03:00}
GIT_TIMESPANCOMMAND_TIMESPAN="$(date "+%F ${GIT_TIMESPAN_MIDNIGHT}")" || GIT_TIMESPANCOMMAND_TIMESPAN='yesterday'
export GIT_TIMESPANCOMMAND_TIMESPAN
GIT_TIMESPANCOMMAND_WHEN=today exec git-timespan-command "$@"
