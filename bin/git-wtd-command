#!/bin/bash

: ${GIT_TIMESPAN_MIDNIGHT:=03:00}
: ${GIT_TIMESPAN_WEEKSTARTDAY:=Monday}

GIT_TIMESPANCOMMAND_TIMESPAN="$(date --date "last $GIT_TIMESPAN_WEEKSTARTDAY" +"%F $GIT_TIMESPAN_WEEKSTARTDAY")" exec git-timespan-command "$@"
