#!/bin/sh

unset HUB   # To avoid that git-wrapper also chooses the hub-subchanges wrapper.

git b3rsubchanges --include-pull-requests --qualified-with-userrepo --as markdown "$@" | clipboard --tee --verbose
