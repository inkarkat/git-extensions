#!/bin/bash
set -o pipefail

unset HUB   # To avoid that git-wrapper also chooses the hub-subchanges wrapper.

git ubrsubchanges --include-pull-requests --qualified-with-userrepo --as markdown "$@" | clipboard --tee --verbose
